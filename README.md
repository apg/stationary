# Stationary - a simple static site generator

## Overview

Stationary makes it easy to create a static site.

## Usage

### Templating

By default, jinja2 templates are used. In the future we'll make it
possible to use other templating engines, but for now jinja2 is 
what you get.

